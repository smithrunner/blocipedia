<h1>Welcome to Blocipedia!</h1>

<div class="row">
  <div class="col-md-8">
    <% @wikis.each do |wiki| %>
      <h4 class="media-heading">
        <%= link_to wiki.title, wiki_path(@wiki) %>
      </h4>
    <% end %>
  </div>
</div>
